<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.5; color: #333; margin: 0; padding: 0; }
    .header { background-color: #c00; color: white; padding: 12px 15px; }
    .header h2 { margin: 0; font-size: 18px; }
    .content { padding: 15px; background-color: #f9f9f9; }
    .info { margin-bottom: 10px; }
    table { width: 100%; border-collapse: collapse; background-color: white; }
    th { background-color: #333; color: white; padding: 8px 10px; text-align: left; font-size: 13px; }
    td { padding: 8px 10px; border-bottom: 1px solid #ddd; font-size: 13px; }
    .overdue { color: #c00; font-weight: bold; }
    a { color: #169; }
    .footer { padding: 12px 15px; font-size: 12px; color: #666; border-top: 1px solid #ddd; background-color: #f9f9f9; }
  </style>
</head>
<body>
  <div class="header">
    <h2><%= @reminder_type == :second ? '⚠️ Eskalasyon: Geciken İş' : '⏰ Geciken İş Hatırlatması' %></h2>
  </div>
  <div class="content">
    <div class="info">
      <p style="margin: 0 0 5px 0;">Merhaba <strong><%= @recipient.firstname %>,</strong></p>
      <% if @reminder_type == :second && @assignee %>
        <p style="margin: 0 0 5px 0; color: #c00;">Bu, <strong><%= @assignee.name %></strong> kullanıcısına atanan iş için eskalasyon bildirimidir.</p>
      <% end %>
      <p style="margin: 0;">Aşağıdaki iş <span class="overdue"><%= @days_overdue %> gün gecikmiştir</span>:</p>
    </div>
    
    <table>
      <tr>
        <th>Proje</th>
        <th>ID</th>
        <th>İş Tipi</th>
        <th>Konu</th>
        <th>Bitiş Tarihi</th>
        <th>Gecikme</th>
      </tr>
      <tr>
        <td><%= @issue.project.name %></td>
        <td><a href="<%= @issue_url %>">#<%= @issue.id %></a></td>
        <td><%= @issue.tracker.name %></td>
        <td><%= @issue.subject %></td>
        <td><%= @issue.due_date %></td>
        <td class="overdue"><%= @days_overdue %> gün</td>
      </tr>
    </table>
    
    <% if @issue.assigned_to %>
      <p style="margin: 10px 0 0 0;"><strong>Atanan:</strong> <%= @issue.assigned_to.name %></p>
    <% end %>
  </div>
  <div class="footer">
    Bu e-posta Redmine Hatırlatma Plugin'i tarafından otomatik gönderilmiştir.
  </div>
</body>
</html>
