<%= @reminder_type == :second ? '[ESKALASYON] ' : '' %>Geciken İş Hatırlatması

Merhaba <%= @recipient.firstname %>,

<% if @reminder_type == :second && @assignee %>
Bu, <%= @assignee.name %> kullanıcısına atanan iş için eskalasyon bildirimidir.
<% end %>

Aşağıdaki iş <%= @days_overdue %> gün gecikmiştir:

Proje: <%= @issue.project.name %>
ID: #<%= @issue.id %> (<%= @issue_url %>)
İş Tipi: <%= @issue.tracker.name %>
Konu: <%= @issue.subject %>
Bitiş Tarihi: <%= @issue.due_date %>
Gecikme: <%= @days_overdue %> gün
<% if @issue.assigned_to %>Atanan: <%= @issue.assigned_to.name %><% end %>

--
Bu e-posta Redmine Hatırlatma Plugin'i tarafından otomatik gönderilmiştir.
